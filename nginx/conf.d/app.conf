# ── Step 1: works immediately over plain HTTP, no domain/cert needed ──
server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    location ~* \.(css|js|jpg|jpeg|png|svg|ico)$ {
        expires 7d;
        add_header Cache-Control "public";
    }

    gzip on;
    gzip_types text/plain text/css application/javascript image/svg+xml;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
}

# ── Step 2: once you own a domain pointed at this VPS, run certbot
#    (see DEPLOY.md), then delete the "listen 80" block above, replace
#    server_name _ with yourdomain.com in it (keep it as an HTTP->HTTPS
#    redirect), and uncomment the HTTPS block below. ──

# server {
#     listen 443 ssl;
#     server_name yourdomain.com;
#
#     ssl_certificate     /etc/letsencrypt/live/yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/yourdomain.com/privkey.pem;
#
#     root /usr/share/nginx/html;
#     index index.html;
#
#     location / {
#         try_files $uri $uri/ =404;
#     }
#
#     location ~* \.(css|js|jpg|jpeg|png|svg|ico)$ {
#         expires 7d;
#         add_header Cache-Control "public";
#     }
#
#     gzip on;
#     gzip_types text/plain text/css application/javascript image/svg+xml;
#
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header Referrer-Policy "strict-origin-when-cross-origin" always;
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
# }
